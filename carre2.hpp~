#include <iostream>
#include<vector>
using namespace std;
class Carre{
   
  public:
  int taille;
  vector<double> tab;
  Carre();
  Carre(int); 

};

ostream &operator<<(ostream &out, Carre &tab);
{
 for(int i=1, i<tab.taille, i++)
   for(int j=1, i<tab.taille, j++)
     return out
    
}

